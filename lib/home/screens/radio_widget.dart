import 'package:flutter/material.dart';

class radio_widet extends StatelessWidget {
  const radio_widet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue);
  }
}
